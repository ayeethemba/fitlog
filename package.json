{
  "name": "fitlog",
  "version": "1.0.0",
  "description": "Full-stack fitness tracker: React + Express + PostgreSQL",
  "private": true,
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "test": "npm --prefix server test",
    "build": "npm --prefix client run build"
  },
  "author": "Themba Chika",
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
